AI_CONTEXT/CHANGELOG.md — летопись изменений и запись генагентов
=============================================================

2026-06-22T00:00:00Z — первый генагент
------------------------------------
Краткое описание деятельности (пересказ, предоставлен пользователем):
- Создал `index.html` с canvas, растягиваемым на весь экран, и загрузчик `liblist`.
- Создал папку `lib/` и файл `liblist`.
- Добавил пустой модуль `lib/Sample.js`.
- Добавил `lib/CubeSample.js` — пример рендеринга вращающегося куба.
- Создал `lib/MapTiler.js` (первоначально назывался `World.js`) и `lib/Game.js` — движок/исполнитель.
- Реализовал типы тайлов (flat/full) и набор вариантов (grass, water, stone, log и пр.).
- Добавил `lib/MapGen.js` — генератор случайного мира, вызывающий `MapTiler`.
- Добавил `lib/Camera.js` — управление камерой: зум колесом мыши и перемещение клавишами (WASD/стрелки) с ускорением при Shift.
- Создал систему сущностей: `lib/Entity.js`, `lib/EntitySlime.js`, `lib/EntitySlimeSample.js`.
- Добавил спавн слаймов в `MapGen.js`, затем вынес логику в `lib/EntitySpawner.js`.

Файлы, созданные/изменённые первым генагентом (неполный список):
- `index.html`
- `liblist`
- `lib/Sample.js`
- `lib/CubeSample.js`
- `lib/MapTiler.js`
- `lib/Game.js`
- `lib/MapGen.js`
- `lib/Camera.js`
- `lib/Entity.js`
- `lib/EntitySlime.js`
- `lib/EntitySlimeSample.js`
- `lib/EntitySpawner.js`

Комментарий: запись восстановлена по устному пересказу; точные временные метки и отдельные правки — в оригинальных коммитах (если доступны).

2026-06-22T00:00:00Z — второй генагент
-----------------------------------
Детальное описание внесённых изменений (этот чейнджлог создаётся автоматически при фиксации):

Что сделано:
- Создана папка `AI_CONTEXT/` в корне репозитория.
- Добавлен `AI_CONTEXT/README.md` — статичный свод правил для генагентов (правила редактирования, ритуал чтения и правила возникновения генагента).
- Добавлен `AI_CONTEXT/SUMMARY.md` — краткие заметки по всем ключевым файлам репозитория (файл предназначен для частого обновления агентами).
- Удалён `lib/README_lib.txt` (его содержимое перенесено в `AI_CONTEXT/README.md` и `AI_CONTEXT/SUMMARY.md`).
- Обновлён `AI_CONTEXT/README.md` для уточнения правил: добавлены требование актуализации `SUMMARY.md` при добавлении записей в чейнджлог; правила содержимого папки `lib/`; разрешение генагентам оставлять личностные записи и создавать файлы в `AI_CONTEXT/`.

Файлы, созданные/изменённые вторым генагентом:
- Создан: `AI_CONTEXT/README.md`
- Создан: `AI_CONTEXT/SUMMARY.md`
- Создан: `AI_CONTEXT/CHANGELOG.md` (эта запись)
- Удалён: `lib/README_lib.txt`
- Изменён: `AI_CONTEXT/README.md` (несколько правок для минимизации и уточнений)
- Изменён: `AI_CONTEXT/SUMMARY.md` (добавлены заметки о действиях генагента и текущем состоянии)

Требования, наложенные данной записью:
- При добавлении этой записи в `AI_CONTEXT/CHANGELOG.md` обязуется актуализировать `AI_CONTEXT/SUMMARY.md` (требование выполнено).

Пояснение:
Эта запись фиксирует переход проекта к паттерну «контекст + саммари»: правила и статичные политики — в `AI_CONTEXT/README.md`; динамичные и подробные описания — в `AI_CONTEXT/SUMMARY.md`.